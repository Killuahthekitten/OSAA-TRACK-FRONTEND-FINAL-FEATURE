# Base URL of the backend API. The Vite dev server also proxies /api to
# this same address (see vite.config.js), so this is mainly used for
# production builds where the frontend is served separately from the API.
VITE_API_BASE_URL=http://localhost:5000
